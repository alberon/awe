# Compiled JavaScript (run `grunt build` or `grunt lib`)
/lib-build/

# Compiled man pages (run `grunt build` or `grunt man`)
/man-build/

# Compiled documentation (run `grunt build` or `grunt docs` for HTML; `grunt pdfdocs` for PDF)
/docs-html/
/docs-pdf/

# Output from test scripts (run `grunt test`) and sample builds
/fixtures/*/build/
/sample/*/build/

# Ruby & Node packages
/.bundle/
/node_modules/
/ruby_bundle/

# Sublime Text workspace
/*.sublime-workspace

# npm debug log
npm-debug.log
